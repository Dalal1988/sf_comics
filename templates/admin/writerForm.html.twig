{% extends "admin/base.html.twig" %}

{% block main %}


         {{form_start(writerForm)}}

                {{form_label(writerForm.name, 'Name')}}
                {{form_widget(writerForm.name)}}

                {{form_label(writerForm.country, 'Country')}}
                {{form_widget(writerForm.country)}}


                {{form_widget(writerForm.Submit)}}

	{{form_end(writerForm)}}

{% endblock %}
