{% extends "base.html.twig" %}

{% block body %}


		{% for comic in comics %}
			
			<h2>{{ comic.title }}</h2>
			<h3>{{ comic.description }}</h3>
			<h3>{{ comic.year | date('d-m-Y') }}</h3>

		{% endfor %}

{% endblock %}
